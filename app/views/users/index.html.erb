<% provide(:title, 'すべてのユーザー画面') %>


<div class="row">
<div class="col-md-8 col-md-offset-2">
  <div class="title"><p>すべてのユーザー</p></div>
<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
  <%= will_paginate class: "center", previous_label: '&lt 前へ', next_label: '次へ &gt'%>
</ul>

</div>

</div>
